# Local preview

From the repository root, run:

```powershell
npm --prefix tools/local-preview start
```

Then open <http://127.0.0.1:4173/>. Stop the server with `Ctrl+C`.

Set `PREVIEW_PORT` before starting to use another port.
